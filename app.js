const I18N = {
  ja: {
    pageTitle: 'サイゼリヤ - デジタルトラットリア',
    navFindStores: '店舗検索',
    navMenu: 'メニュー',
    navFavorites: 'お気に入り',
    navHistory: '履歴',
    signIn: 'サインイン',
    homeTitle: 'ようこそ、<br><span>デジタルトラットリアへ。</span>',
    homeSubtitle: '近くのサイゼリヤを探して、家族で気軽にイタリアンを楽しみましょう。',
    homeSearchPlaceholder: 'エリア・駅名で検索...',
    homeFindBtn: '店舗を探す',
    homeNearbyBtn: '近くのお店',
    quickDiscoveriesTitle: 'おすすめ機能',
    quickMapTitle: '地図で探す',
    quickMapDesc: '地図から店舗を直感的に探して、次のお食事にぴったりのお店を見つけましょう。',
    quickMapLink: '地図を開く',
    quickLunchTitle: 'ランチメニュー',
    quickLunchDesc: '平日15時までのお得なセットをご用意しています。',
    quickTakeoutTitle: 'テイクアウト',
    quickTakeoutDesc: 'ご自宅でもサイゼリヤを。受け取り対応店舗を探せます。',
    quickTakeoutLink: '対応店舗を探す',
    searchTitle: '店舗を探す',
    listSearchPlaceholder: '市区町村・エリア・駅名で検索',
    filterParking: '駐車場',
    filterWifi: 'Wi-Fi',
    filterEpayment: '電子決済',
    filterMore: 'その他',
    breadcrumbHome: 'ホーム',
    breadcrumbSearch: '店舗検索',
    detailBannerTag: 'ゆったり席あり',
    detailHoursTitle: '<span class="material-symbols-outlined">schedule</span> 営業時間',
    detailTodayLabel: '本日',
    detailLastOrderLabel: 'ラストオーダー',
    detailCrowdLevelLabel: '混雑度',
    detailFacilitiesTitle: '<span class="material-symbols-outlined">storefront</span> 設備',
    detailLocationTitle: '店舗情報',
    detailLargeMapLink: '大きな地図で見る',
    detailRouteBtn: 'ルート検索',
    menuHighlightTitle: 'おすすめメニュー',
    menuLoadingText: 'メニュー情報を取得中です...',
    dashboardTitle: 'ダッシュボード',
    dashboardSubtitle: '保存した店舗と最近見た店舗にすぐアクセスできます。',
    dashboardFavoritesTitle: '<span class="material-symbols-outlined">favorite</span> お気に入り店舗',
    dashboardManage: '管理 <span class="material-symbols-outlined" style="font-size: 14px; vertical-align: middle;">chevron_right</span>',
    dashboardHistoryTitle: '<span class="material-symbols-outlined">history</span> 最近見た店舗',
    dashboardFindMoreBtn: 'さらに店舗を探す',
    footerPrivacy: 'プライバシーポリシー',
    footerTerms: '利用規約',
    footerContact: 'お問い合わせ',
    footerCorporate: '企業情報',
    emptyStoreList: '条件に一致する店舗が見つかりませんでした。',
    mapViewDetails: '詳細を見る',
    detailPrefix: 'サイゼリヤ',
    statusOpen: '営業中',
    statusClosed: '営業時間外',
    statusClosesSoon: 'まもなく閉店',
    crowdBusy: '混雑',
    crowdNormal: '通常',
    crowdBusyDesc: '10〜15分程度の待ち時間が発生する見込みです。',
    crowdNormalDesc: '比較的スムーズにご案内できます。',
    menuLoading: 'メニュー情報を取得中です...',
    menuLoadError: 'メニュー情報を読み込めませんでした。時間をおいて再度お試しください。',
    viewedJustNow: '今見た店舗',
    viewedHoursAgo: '2時間前に閲覧',
    viewedYesterday: '昨日閲覧',
    noRecent: '最近見た店舗はありません。',
    addAnotherFavorite: 'お気に入りを追加',
    addAnotherFavoriteDesc: 'よく行く店舗を保存して、メニューや営業時間にすぐアクセスできます。'
  },
  en: {
    pageTitle: 'Saizeriya - Digital Trattoria',
    navFindStores: 'Find Stores',
    navMenu: 'Menu',
    navFavorites: 'Favorites',
    navHistory: 'History',
    signIn: 'Sign In',
    homeTitle: 'Welcome to your<br><span>Digital Trattoria.</span>',
    homeSubtitle: 'Find your nearest Saizeriya and enjoy casual, reliable Italian dining for the whole family.',
    homeSearchPlaceholder: 'Search by area or station...',
    homeFindBtn: 'Find Stores',
    homeNearbyBtn: 'Near Me',
    quickDiscoveriesTitle: 'Quick Discoveries',
    quickMapTitle: 'Explore Area Map',
    quickMapDesc: 'Browse our locations interactively to find the perfect spot for your next meal.',
    quickMapLink: 'View Map',
    quickLunchTitle: 'Lunch Menu Specials',
    quickLunchDesc: 'Great value sets available weekdays until 3 PM.',
    quickTakeoutTitle: 'Order Takeout',
    quickTakeoutDesc: 'Enjoy Saizeriya at home. Find stores offering convenient pickup services.',
    quickTakeoutLink: 'Find Takeout Stores',
    searchTitle: 'Find a Trattoria',
    listSearchPlaceholder: 'Search by city, neighborhood, or station',
    filterParking: 'Parking',
    filterWifi: 'Wi-Fi',
    filterEpayment: 'E-Payments',
    filterMore: 'More',
    breadcrumbHome: 'Home',
    breadcrumbSearch: 'Find Stores',
    detailBannerTag: 'Spacious Seating',
    detailHoursTitle: '<span class="material-symbols-outlined">schedule</span> Business Hours',
    detailTodayLabel: 'Today',
    detailLastOrderLabel: 'Last Order',
    detailCrowdLevelLabel: 'CROWD LEVEL',
    detailFacilitiesTitle: '<span class="material-symbols-outlined">storefront</span> Facilities',
    detailLocationTitle: 'Location Details',
    detailLargeMapLink: 'View on large map',
    detailRouteBtn: 'Route Search',
    menuHighlightTitle: 'Menu Highlight',
    menuLoadingText: 'Fetching latest menu from server...',
    dashboardTitle: 'Your Dashboard',
    dashboardSubtitle: 'Quick access to your saved locations and recent views.',
    dashboardFavoritesTitle: '<span class="material-symbols-outlined">favorite</span> Favorite Stores',
    dashboardManage: 'Manage <span class="material-symbols-outlined" style="font-size: 14px; vertical-align: middle;">chevron_right</span>',
    dashboardHistoryTitle: '<span class="material-symbols-outlined">history</span> Recently Viewed',
    dashboardFindMoreBtn: 'Find More Stores',
    footerPrivacy: 'Privacy Policy',
    footerTerms: 'Terms of Service',
    footerContact: 'Contact Us',
    footerCorporate: 'Corporate Info',
    emptyStoreList: 'No stores found matching your criteria.',
    mapViewDetails: 'View Details',
    detailPrefix: 'Saizeriya',
    statusOpen: 'Open',
    statusClosed: 'Closed',
    statusClosesSoon: 'Closes Soon',
    crowdBusy: 'Busy',
    crowdNormal: 'Normal',
    crowdBusyDesc: 'Expect a 10-15 minute wait for tables.',
    crowdNormalDesc: 'Seating is currently moving smoothly.',
    menuLoading: 'Fetching latest menu from server...',
    menuLoadError: 'Could not load menu data at this time. Please try again later.',
    viewedJustNow: 'Viewed just now',
    viewedHoursAgo: 'Viewed 2 hours ago',
    viewedYesterday: 'Viewed yesterday',
    noRecent: 'No recently viewed stores.',
    addAnotherFavorite: 'Add another favorite',
    addAnotherFavoriteDesc: 'Save locations you visit frequently for quick access to menus and hours.'
  }
};

const STORE_LOCALE = {
  s1: { ja: { name: '新宿西口店' }, en: { name: 'Shinjuku West Exit' } },
  s2: { ja: { name: '新宿南口店' }, en: { name: 'Shinjuku South' } },
  s3: { ja: { name: '代々木駅前店' }, en: { name: 'Yoyogi Station Front' } },
  s4: { ja: { name: '渋谷センター街店' }, en: { name: 'Shibuya Center Gai' } },
  s5: { ja: { name: '六本木ヒルズ店' }, en: { name: 'Roppongi Hills' } },
  s6: { ja: { name: '池袋サンシャイン店' }, en: { name: 'Ikebukuro Sunshine' } }
};

const MENU_LOCALE = {
  categories: {
    Salads: { ja: 'サラダ', en: 'Salads' },
    Appetizers: { ja: '前菜', en: 'Appetizers' },
    'Pizza & Pasta': { ja: 'ピザ・パスタ', en: 'Pizza & Pasta' },
    'Doria & Gratin': { ja: 'ドリア・グラタン', en: 'Doria & Gratin' }
  },
  items: {
    m1: { ja: { name: 'ミラノサラミのサラダ', description: 'シャキシャキのレタスにミラノサラミとペコリーノチーズをトッピング。' } },
    m2: { ja: { name: '小エビのサラダ', description: 'ボイルした小エビをのせた定番サラダです。' } },
    m3: { ja: { name: 'エスカルゴのオーブン焼き', description: 'ガーリックバターで焼き上げた人気の前菜です。' } },
    m4: { ja: { name: '辛味チキン', description: 'スパイシーな味付けで焼き上げたジューシーなチキン。' } },
    m5: { ja: { name: 'マルゲリータピザ', description: 'トマトソース、モッツァレラ、バジルの定番ピザ。' } },
    m6: { ja: { name: 'ミートソースボロニア風', description: 'コクのあるミートソースのスパゲッティ。' } },
    m7: { ja: { name: 'ミラノ風ドリア', description: 'ミートソースとホワイトソースを重ねて焼き上げた人気商品。' } }
  }
};

const app = {
  currentView: 'home',
  map: null,
  markers: [],
  favorites: JSON.parse(localStorage.getItem('saizeriya_favorites')) || [],
  history: JSON.parse(localStorage.getItem('saizeriya_history')) || [],
  currentStoreDetail: null,
  language: localStorage.getItem('saizeriya_language') || 'ja',

  init() {
    this.applyLanguage();
    this.navigate('home');
    this.renderFavorites();
    this.renderHistory();
  },

  t(key) {
    return (I18N[this.language] && I18N[this.language][key]) || I18N.ja[key] || key;
  },

  localizedStore(store) {
    const locale = STORE_LOCALE[store.id] && STORE_LOCALE[store.id][this.language] ? STORE_LOCALE[store.id][this.language] : {};
    return {
      ...store,
      name: locale.name || store.name
    };
  },

  statusLabel(status) {
    if (status === 'Open') return this.t('statusOpen');
    if (status === 'Closed') return this.t('statusClosed');
    return this.t('statusClosesSoon');
  },

  facilityLabel(key) {
    const fac = FACILITY_ICONS[key];
    if (!fac) return key;
    if (this.language === 'ja') {
      const jaMap = {
        'drink-bar': 'ドリンクバー',
        'non-smoking': '禁煙',
        'smoking-room': '喫煙室',
        'free-wifi': '無料Wi-Fi',
        'parking': '駐車場',
        'e-payments': '電子決済'
      };
      return jaMap[key] || fac.label;
    }
    return fac.label;
  },

  applyLanguage() {
    document.documentElement.lang = this.language;
    document.title = this.t('pageTitle');

    const textMap = {
      'nav-find-stores': 'navFindStores',
      'nav-menu': 'navMenu',
      'nav-favorites': 'navFavorites',
      'nav-history': 'navHistory',
      'btn-sign-in': 'signIn',
      'home-subtitle': 'homeSubtitle',
      'home-find-btn': 'homeFindBtn',
      'home-nearby-btn': 'homeNearbyBtn',
      'quick-discoveries-title': 'quickDiscoveriesTitle',
      'quick-map-title': 'quickMapTitle',
      'quick-map-desc': 'quickMapDesc',
      'quick-map-link': 'quickMapLink',
      'quick-lunch-title': 'quickLunchTitle',
      'quick-lunch-desc': 'quickLunchDesc',
      'quick-takeout-title': 'quickTakeoutTitle',
      'quick-takeout-desc': 'quickTakeoutDesc',
      'quick-takeout-link': 'quickTakeoutLink',
      'search-title': 'searchTitle',
      'filter-parking': 'filterParking',
      'filter-wifi': 'filterWifi',
      'filter-epayment': 'filterEpayment',
      'filter-more': 'filterMore',
      'breadcrumb-home': 'breadcrumbHome',
      'breadcrumb-search': 'breadcrumbSearch',
      'detail-banner-tag': 'detailBannerTag',
      'detail-today-label': 'detailTodayLabel',
      'detail-last-order-label': 'detailLastOrderLabel',
      'detail-crowd-level-label': 'detailCrowdLevelLabel',
      'detail-location-title': 'detailLocationTitle',
      'detail-large-map-link': 'detailLargeMapLink',
      'detail-route-btn': 'detailRouteBtn',
      'menu-highlight-title': 'menuHighlightTitle',
      'menu-loading-text': 'menuLoadingText',
      'dashboard-title': 'dashboardTitle',
      'dashboard-subtitle': 'dashboardSubtitle',
      'dashboard-find-more-btn': 'dashboardFindMoreBtn',
      'global-menu-title': 'navMenu',
      'footer-privacy': 'footerPrivacy',
      'footer-terms': 'footerTerms',
      'footer-contact': 'footerContact',
      'footer-corporate': 'footerCorporate'
    };

    Object.entries(textMap).forEach(([id, key]) => {
      const el = document.getElementById(id);
      if (el) el.textContent = this.t(key);
    });

    const htmlMap = {
      'home-title': 'homeTitle',
      'detail-hours-title': 'detailHoursTitle',
      'detail-facilities-title': 'detailFacilitiesTitle',
      'dashboard-favorites-title': 'dashboardFavoritesTitle',
      'dashboard-history-title': 'dashboardHistoryTitle',
      'dashboard-manage': 'dashboardManage'
    };

    Object.entries(htmlMap).forEach(([id, key]) => {
      const el = document.getElementById(id);
      if (el) el.innerHTML = this.t(key);
    });

    const homeInput = document.getElementById('home-search-input');
    if (homeInput) homeInput.placeholder = this.t('homeSearchPlaceholder');
    const listInput = document.getElementById('list-search-input');
    if (listInput) listInput.placeholder = this.t('listSearchPlaceholder');

    const selector = document.getElementById('language-select');
    if (selector) selector.value = this.language;
  },

  changeLanguage(lang) {
    this.language = lang === 'en' ? 'en' : 'ja';
    localStorage.setItem('saizeriya_language', this.language);
    this.applyLanguage();

    if (this.currentView === 'search') {
      this.filterStores();
    }
    if (this.currentStoreDetail) {
      this.viewStoreDetail(this.currentStoreDetail.id, false);
    }
    this.renderFavorites();
    this.renderHistory();
  },

  navigate(viewId) {
    document.querySelectorAll('.view-section').forEach(el => el.classList.remove('active'));
    document.getElementById(`view-${viewId}`).classList.add('active');

    document.querySelectorAll('.nav-link').forEach(el => {
      if (el.dataset.target === viewId) el.classList.add('active');
      else el.classList.remove('active');
    });

    this.currentView = viewId;

    if (viewId === 'search') {
      if (!this.map) this.initMap();
      this.renderStoreList(STORES);
      setTimeout(() => {
        if (this.map && typeof google !== 'undefined') {
          google.maps.event.trigger(this.map, 'resize');
        }
      }, 100);
    } else if (viewId === 'dashboard') {
      this.renderFavorites();
      this.renderHistory();
    } else if (viewId === 'menu') {
      this.fetchMenu('global-menu-container');
    }

    window.scrollTo(0, 0);
  },

  handleHomeSearch(e) {
    if (e.key === 'Enter') {
      const val = e.target.value;
      this.navigate('search');
      document.getElementById('list-search-input').value = val;
      this.filterStores();
    }
  },

  initMap() {
    this.map = new google.maps.Map(document.getElementById('map'), {
      center: { lat: 35.6895, lng: 139.6917 },
      zoom: 12,
      mapTypeControl: false,
      streetViewControl: false
    });
    this.infoWindow = new google.maps.InfoWindow();
  },

  renderStoreList(storesToRender) {
    const container = document.getElementById('store-list-container');
    container.innerHTML = '';

    this.markers.forEach(m => m.setMap(null));
    this.markers = [];

    if (storesToRender.length === 0) {
      container.innerHTML = `<div class="empty-state">${this.t('emptyStoreList')}</div>`;
      return;
    }

    const bounds = new google.maps.LatLngBounds();

    storesToRender.forEach(store => {
      const localized = this.localizedStore(store);
      const card = document.createElement('div');
      card.className = 'store-card';
      card.onclick = () => this.viewStoreDetail(store.id);

      const facilitiesHtml = store.facilities.map(f => {
        const fac = FACILITY_ICONS[f];
        return `<div class="facility-icon" title="${this.facilityLabel(f)}">
                  <span class="material-symbols-outlined">${fac.icon}</span>
                </div>`;
      }).join('');

      card.innerHTML = `
        <div class="store-card-header">
          <h3>${localized.name}</h3>
          <span class="status-badge status-${store.status.replace(' ', '')}">${this.statusLabel(store.status)}</span>
        </div>
        <div class="store-distance">${store.distance}</div>
        <div class="store-address">${store.address}</div>
        <div class="store-facilities">
          ${facilitiesHtml}
        </div>
      `;

      card.addEventListener('mouseenter', () => {
        card.classList.add('highlighted');
        const marker = this.markers.find(m => m.storeId === store.id);
        if (marker && this.infoWindow) {
          this.infoWindow.setContent(`
            <div style="text-align:center;">
              <h3 style="margin-bottom: 4px; color: #333;">${localized.name}</h3>
              <p style="margin-bottom: 8px; color: #666;">${this.statusLabel(store.status)}</p>
              <button style="background:var(--saizeriya-green); color:white; border:none; padding:4px 12px; border-radius:12px; cursor:pointer;" onclick="app.viewStoreDetail('${store.id}')">${this.t('mapViewDetails')}</button>
            </div>
          `);
          this.infoWindow.open(this.map, marker);
        }
      });
      card.addEventListener('mouseleave', () => {
        card.classList.remove('highlighted');
        if (this.infoWindow) {
          this.infoWindow.close();
        }
      });

      container.appendChild(card);

      const customIcon = {
        path: google.maps.SymbolPath.CIRCLE,
        fillColor: '#007a33',
        fillOpacity: 1,
        strokeColor: 'white',
        strokeWeight: 3,
        scale: 10
      };

      const marker = new google.maps.Marker({
        position: { lat: store.lat, lng: store.lng },
        map: this.map,
        icon: customIcon,
        title: localized.name
      });
      marker.storeId = store.id;

      marker.addListener('click', () => {
        if (this.infoWindow) {
          this.infoWindow.setContent(`
            <div style="text-align:center;">
              <h3 style="margin-bottom: 4px; color: #333;">${localized.name}</h3>
              <p style="margin-bottom: 8px; color: #666;">${this.statusLabel(store.status)}</p>
              <button style="background:var(--saizeriya-green); color:white; border:none; padding:4px 12px; border-radius:12px; cursor:pointer;" onclick="app.viewStoreDetail('${store.id}')">${this.t('mapViewDetails')}</button>
            </div>
          `);
          this.infoWindow.open(this.map, marker);
        }
      });

      this.markers.push(marker);
      bounds.extend({ lat: store.lat, lng: store.lng });
    });

    if (this.markers.length > 0) this.map.fitBounds(bounds, 50);
  },

  filterStores() {
    const query = document.getElementById('list-search-input').value.toLowerCase();
    const activeFilters = Array.from(document.querySelectorAll('.filter-btn.active'))
      .map(btn => btn.dataset.facility)
      .filter(f => f);

    const filtered = STORES.filter(store => {
      const localizedName = this.localizedStore(store).name.toLowerCase();
      const matchesText = localizedName.includes(query) || store.name.toLowerCase().includes(query) || store.address.toLowerCase().includes(query);
      const matchesFacilities = activeFilters.every(f => store.facilities.includes(f));
      return matchesText && matchesFacilities;
    });
    this.renderStoreList(filtered);
  },

  viewStoreDetail(storeId, updateHistory = true) {
    const store = STORES.find(s => s.id === storeId);
    if (!store) return;

    const localized = this.localizedStore(store);
    this.currentStoreDetail = store;

    if (updateHistory) {
      this.history = this.history.filter(id => id !== storeId);
      this.history.unshift(storeId);
      if (this.history.length > 4) this.history.pop();
      localStorage.setItem('saizeriya_history', JSON.stringify(this.history));
    }

    document.getElementById('detail-bc-name').textContent = localized.name;
    document.getElementById('detail-name').textContent = `${this.t('detailPrefix')} ${localized.name}`;
    document.getElementById('detail-hero-image').src = store.heroImage;
    document.getElementById('detail-hours').textContent = store.hours;
    document.getElementById('detail-last-order').textContent = store.lastOrder;
    document.getElementById('detail-address').textContent = store.address;
    document.getElementById('detail-phone').textContent = store.phone;
    document.getElementById('detail-distance').textContent = store.distance;

    const badge = document.getElementById('detail-status-badge');
    badge.className = `status-badge status-${store.status.replace(' ', '')}`;
    const statusLabel = this.statusLabel(store.status);
    badge.innerHTML = store.status === 'Open' ? `<span class="material-symbols-outlined" style="font-size:16px;">check_circle</span> ${statusLabel}` : statusLabel;

    const isBusy = store.crowdLevel > 70;
    document.getElementById('detail-crowd-status').textContent = isBusy ? this.t('crowdBusy') : this.t('crowdNormal');
    document.getElementById('detail-crowd-status').style.color = isBusy ? '#dc3545' : '#28a745';
    document.getElementById('detail-crowd-desc').textContent = isBusy ? this.t('crowdBusyDesc') : this.t('crowdNormalDesc');

    const crowdBar = document.getElementById('detail-crowd-bar');
    crowdBar.innerHTML = '';
    const segments = 5;
    const filledSegments = Math.ceil((store.crowdLevel / 100) * segments);
    for (let i = 0; i < segments; i++) {
      const seg = document.createElement('div');
      seg.className = `crowd-segment ${i < filledSegments ? 'filled' : ''}`;
      if (isBusy && i < filledSegments) seg.style.background = '#dc3545';
      else if (i < filledSegments) seg.style.background = '#28a745';
      crowdBar.appendChild(seg);
    }

    const facContainer = document.getElementById('detail-facilities');
    facContainer.innerHTML = store.facilities.map(f => {
      const fac = FACILITY_ICONS[f];
      return `
        <div class="facility-item">
          <span class="material-symbols-outlined">${fac.icon}</span>
          ${this.facilityLabel(f)}
        </div>
      `;
    }).join('');

    this.updateFavoriteButton();
    this.fetchMenu();
    this.navigate('detail');
  },

  toggleFavorite() {
    if (!this.currentStoreDetail) return;
    const id = this.currentStoreDetail.id;
    const index = this.favorites.indexOf(id);

    if (index > -1) this.favorites.splice(index, 1);
    else this.favorites.push(id);

    localStorage.setItem('saizeriya_favorites', JSON.stringify(this.favorites));
    this.updateFavoriteButton();
  },

  updateFavoriteButton() {
    const btn = document.getElementById('btn-favorite');
    if (!btn || !this.currentStoreDetail) return;

    const isFav = this.favorites.includes(this.currentStoreDetail.id);
    if (isFav) {
      btn.classList.add('active');
      btn.innerHTML = '<span class="material-symbols-outlined" style="font-variation-settings: \'FILL\' 1;">favorite</span>';
    } else {
      btn.classList.remove('active');
      btn.innerHTML = '<span class="material-symbols-outlined">favorite_border</span>';
    }
  },

  async fetchMenu(containerId = 'menu-container') {
    const container = document.getElementById(containerId);
    if (!container) return;
    container.innerHTML = `
      <div class="loading-spinner">
        <span class="material-symbols-outlined" style="animation: spin 1s linear infinite;">sync</span>
        <p>${this.t('menuLoading')}</p>
      </div>
    `;

    try {
      await new Promise(resolve => setTimeout(resolve, 800)); // Simulate loading delay
      const menuData = MENU_DATA;

      container.innerHTML = '';

      menuData.forEach(category => {
        const catDiv = document.createElement('div');
        catDiv.className = 'menu-category';
        const localizedCategory = (MENU_LOCALE.categories[category.category] && MENU_LOCALE.categories[category.category][this.language]) || category.category;

        const itemsHtml = category.items.map(item => {
          const localizedItem = MENU_LOCALE.items[item.id] && MENU_LOCALE.items[item.id][this.language] ? MENU_LOCALE.items[item.id][this.language] : item;
          return `
          <div class="menu-item">
            <img src="${item.image}" alt="${localizedItem.name || item.name}">
            <div class="menu-item-content">
              <div class="menu-item-header">
                <h4>${localizedItem.name || item.name}</h4>
                <span class="menu-price">${item.price}</span>
              </div>
              <p class="menu-desc">${localizedItem.description || item.description}</p>
            </div>
          </div>
        `;
        }).join('');

        catDiv.innerHTML = `
          <h3>${localizedCategory}</h3>
          <div class="menu-grid">
            ${itemsHtml}
          </div>
        `;
        container.appendChild(catDiv);
      });
    } catch (error) {
      console.error(error);
      container.innerHTML = `
        <div class="empty-state">
          <span class="material-symbols-outlined" style="font-size: 48px; color: #dc3545;">error</span>
          <p style="margin-top: 1rem;">${this.t('menuLoadError')}</p>
        </div>
      `;
    }
  },

  renderFavorites() {
    const container = document.getElementById('favorites-grid');
    if (!container) return;

    container.innerHTML = '';

    this.favorites.forEach(id => {
      const store = STORES.find(s => s.id === id);
      if (!store) return;
      const localized = this.localizedStore(store);

      const tags = store.facilities.slice(0, 2).map(f => `<span class="fav-tag">${this.facilityLabel(f)}</span>`).join('');

      const card = document.createElement('div');
      card.className = 'fav-card';
      card.onclick = () => this.viewStoreDetail(store.id);

      card.innerHTML = `
        <div class="fav-btn-absolute" onclick="event.stopPropagation(); app.currentStoreDetail = {id: '${store.id}'}; app.toggleFavorite(); app.renderFavorites();">
          <span class="material-symbols-outlined" style="font-variation-settings: 'FILL' 1;">favorite</span>
        </div>
        <img src="${store.heroImage}" alt="${localized.name}">
        <div class="fav-card-content">
          <div class="fav-card-header">
            <h3>${localized.name}</h3>
            <span class="status-badge status-${store.status.replace(' ', '')}" style="font-size: 0.7rem; padding: 0.15rem 0.5rem;">${this.statusLabel(store.status)}</span>
          </div>
          <div class="fav-distance">
            <span class="material-symbols-outlined" style="font-size: 16px;">location_on</span> ${store.distance}
          </div>
          <div class="fav-tags">
            ${tags}
          </div>
        </div>
      `;
      container.appendChild(card);
    });

    const addCard = document.createElement('div');
    addCard.className = 'add-fav-card';
    addCard.onclick = () => this.navigate('search');
    addCard.innerHTML = `
      <div class="icon"><span class="material-symbols-outlined">add</span></div>
      <h3>${this.t('addAnotherFavorite')}</h3>
      <p style="font-size: 0.85rem;">${this.t('addAnotherFavoriteDesc')}</p>
    `;
    container.appendChild(addCard);
  },

  renderHistory() {
    const container = document.getElementById('recent-grid');
    if (!container) return;

    container.innerHTML = '';

    if (this.history.length === 0) {
      container.innerHTML = `<div style="grid-column: span 2; color: var(--text-secondary);">${this.t('noRecent')}</div>`;
      return;
    }

    this.history.forEach((id, index) => {
      const store = STORES.find(s => s.id === id);
      if (!store) return;
      const localized = this.localizedStore(store);

      const timeStr = index === 0 ? this.t('viewedJustNow') : (index === 1 ? this.t('viewedHoursAgo') : this.t('viewedYesterday'));

      const card = document.createElement('div');
      card.className = 'recent-card';
      card.onclick = () => this.viewStoreDetail(store.id);

      card.innerHTML = `
        <img src="${store.heroImage}" alt="${localized.name}">
        <div class="recent-info">
          <h4>${localized.name}</h4>
          <p>${timeStr}</p>
        </div>
        <div class="recent-arrow">
          <span class="material-symbols-outlined">arrow_forward</span>
        </div>
      `;
      container.appendChild(card);
    });
  }
};

document.addEventListener('DOMContentLoaded', () => {
  app.init();
});
